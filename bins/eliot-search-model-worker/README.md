# eliot-search-model-worker

**Optional.** Isolated worker for dense embedding, reranking or late-interaction profiles.

Admitted only after lexical and code product acceptance passes, and only when measured benefit exceeds
cost and risk. Removing it must return the system to baseline behavior.
