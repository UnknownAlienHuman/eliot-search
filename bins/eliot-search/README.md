# eliot-search

Standalone CLI over the same daemon, recipes, access compiler and result projector.

It does not open the control journal or the index directly. The local user binding mints a bounded
standalone grant.
