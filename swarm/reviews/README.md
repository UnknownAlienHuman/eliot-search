# Independent package reviews

Canonical layout:

```text
swarm/reviews/<package>/<review-id>.toml
```

A review checks one exact submission against its ticket/context, primary contract, stage obligations,
dependency handoffs, tests, ownership and line budget. Package writers cannot accept their own work.

Use `swarm/REVIEW_RECEIPT_TEMPLATE.md`. This directory currently contains no accepted review.
