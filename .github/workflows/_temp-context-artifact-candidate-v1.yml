name: Temporary context artifact candidate v1 integration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: windows-latest
    steps:
      - name: Bootstrap repair in progress
        shell: pwsh
        run: Write-Host 'Automatic integration is paused while the exact archive fragment is repaired.'
