# T02 — Isolate legacy entrypoints and restore micromodular ownership

**State:** NOT_STARTED; task packet only.  
**Stage:** 0. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T01.

## Observed gap

Primary and snapshot daemons use different owner locks, and the snapshot binary contains an alternative lexical index. Many standalone/dead daemon modules make package size and ownership unverifiable.

## Exclusive write boundary

- `bins/eliot-searchd/**`
- `bins/eliot-search/**`
- `Cargo.toml`
- `swarm/crates.toml`
- `swarm/function-packets.toml`
- `swarm/modules/**`
- `T01-approved exact existing capability destinations (mechanical moves only)`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- bins/eliot-searchd/Cargo.toml
- bins/eliot-searchd/src/entry.rs
- bins/eliot-searchd/src/main.rs
- bins/eliot-searchd/AGENTS.md
- bins/eliot-search/src/entry.rs

## Required change

- Exclude snapshot/BM25 and unused prototype binaries from ordinary product build/install; keep historical fixtures or explicitly isolated experimental targets, never an implicit fallback.
- Apply the T01 approved move map into existing capability owners. Preserve observable semantics and tests; do not add forwarding-only crates or duplicate mutable owners. Split the change into mechanical moves inside this PR before behavior fixes.
- Use one exclusion primitive for every supported target touching the same physical data root. Different filenames must not allow concurrent ownership.
- Update exact dependency/target registries and per-package instructions for moved code. Require a reviewed narrower split if one extraction exceeds the package budget.

## Discriminating tests

- Enumerate cargo metadata targets and default/release artifacts; no alternate indexed backend in the supported profile.
- Start primary then each supported maintenance/daemon target against the same disposable root; second ownership is rejected.
- Baseline equivalence tests before/after extraction; handwritten-line counts include local tests.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p eliot-searchd --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

One supported daemon/CLI path, isolated experiments, nonconflicting ownership and measured package line budgets; no feature is claimed implemented by moving files.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
