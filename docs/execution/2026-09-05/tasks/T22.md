# T22 — Qualify one exact Qdrant server-client-artifact profile

**State:** NOT_STARTED; task packet only.  
**Stage:** 5. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T04.

## Observed gap

No real qualified artifact/profile is wired. Model booleans named independent_idf_corpus or wait_for_mutations are not executed backend evidence.

## Exclusive write boundary

- `qualification/qdrant/**`
- `config/qdrant-profile.toml (new)`
- `Cargo.toml`
- `Cargo.lock`
- `crates/search-index-qdrant/search-qdrant-bridge/Cargo.toml`
- `crates/search-index-qdrant/search-qdrant-supervisor/Cargo.toml`
- `crates/search-index-qdrant/search-qdrant-bridge/tests/live_probe.rs (new)`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- qualification/qdrant/W3_QUALIFICATION.md
- crates/search-index-qdrant/search-qdrant-bridge/FUNCTIONS.md
- crates/search-index-qdrant/search-qdrant-supervisor/FUNCTIONS.md
- Architecture indexed-profile requirements

## Required change

- Select and pin an exact server artifact/build provenance, Rust client/transport and schema/profile for Windows x64 plus test lane. Record checksums/licenses and approved ADR; no latest-at-runtime download.
- Execute disposable live probes for independent IDF population, signed i64 bounds, missing upper bound, named sparse vectors, strict mode, payload indexes, wait/readback and required ordering.
- Verify requested build/platform actually exists and is reproducible; lack of a qualified Windows artifact blocks indexed release, not a silent OS/backend switch.
- Current upstream docs describe per-tenant IDF from 1.19.0; this is a candidate capability lead, not permission to assume all filters or versions qualify.

## Discriminating tests

- Forbidden population insertion does not influence permitted leg scores under the chosen IDF filter.
- Malformed schema/profile and wrong artifact hash/version/architecture fail admission.
- Probes report exact commands, named outcomes and server/client versions; no in-memory stand-in accepted.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p search-qdrant-bridge --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

One approved immutable artifact/profile with executed mandatory probes. No indexed capability can be advertised before this evidence exists.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
