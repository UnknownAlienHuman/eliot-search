# T11 — Switch the primary daemon to atomic redb control state

**State:** NOT_STARTED; task packet only.  
**Stage:** 2. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T10.

## Observed gap

An implemented redb crate does not affect primary serving: source catalog, roots, lifecycle and manifests are still file-backed and multi-record append is non-atomic.

## Exclusive write boundary

- `bins/eliot-searchd/src/control_migration.rs`
- `bins/eliot-searchd/src/direct_store.rs`
- `bins/eliot-searchd/src/secure_direct_store.rs`
- `bins/eliot-searchd/src/secure_direct_store_storage_io.rs`
- `bins/eliot-searchd/src/source_roots.rs`
- `bins/eliot-searchd/src/directory_manifest.rs`
- `bins/eliot-searchd/src/catalog_presence.rs`
- `bins/eliot-searchd/src/maintenance*.rs`
- `crates/search-control-redb/src/migration/**`
- `bins/eliot-searchd/tests/control_migration_process.rs`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- docs/runtime/CONTROL_REDB.md
- bins/eliot-searchd/src/secure_direct_store.rs
- bins/eliot-searchd/src/directory_manifest.rs
- bins/eliot-searchd/src/source_roots.rs
- T10 accepted migration API

## Required change

- Under the T08 owner, apply the T10 verified plan to staged redb, read back exact identities/records/references, then atomically publish one cutover marker.
- Route primary source heads, operation receipts, root registrations and manifest references through redb transactions; derived payloads stay in CAS. Remove dual authoritative writes.
- Migrate every reader including verification, maintenance and inventory; they must never continue reading stale source-events.log after cutover.
- Keep original legacy evidence until explicit cleanup. Every crash boundary opens exactly one authority or quarantines; do not recover currentness from Qdrant or empty tables.

## Discriminating tests

- Real primary binary migration/restart, crash before and after cutover, lost ack, malformed staging database, second owner and rollback-before-cutover.
- Batch all-or-nothing visibility and stable historical readback; repeat exact operation once only.
- Catalog-loss/GC protections and 10,000 short reads with zero durable query writes.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p eliot-searchd --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

Primary runtime demonstrably uses redb for authoritative control; ordinary requests no longer append the legacy journal.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
