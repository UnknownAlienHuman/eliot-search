# T41 — Replace required Python validation with Rust Cargo tooling

**State:** NOT_STARTED; task packet only.  
**Stage:** 7. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T01, T04.

## Observed gap

Required PowerShell validators still invoke Python; deleting them would lose checks, while leaving them makes the agreed Rust/Cargo workflow incomplete.

## Exclusive write boundary

- `tools/**`
- `xtask/** (new integration tooling package; not a capability shell)`
- `Cargo.toml`
- `Cargo.lock`
- `.cargo/config.toml`
- `.github/workflows/**`
- `fixtures/tooling/** (new)`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- tools/validate-architecture-coverage.ps1
- tools/validate-architecture-coverage.py
- tools/validate-swarm.ps1
- tools/validate-function-packets.ps1
- current validator fixture manifests

## Required change

- Inventory actually required validators and their positive/negative fixtures, consolidate duplicated checks, then implement equivalent bounded Rust entrypoints.
- Provide a small explicit Cargo command surface for registry/scope/schema/qualification checks. Do not build another swarm controller or document-report framework inside Search.
- Compare rejection behavior against captured existing fixtures, retire replaced Python entrypoints and remove bytecode/generated caches from tracked source.
- Keep all CI manual-only/read-only and avoid forcing Node/Python setup in clean build/test/package paths. Preserve historical evidence separately from executable tooling.

## Discriminating tests

- Clean environment without python/node can build, test, validate required registries and package supported runtime.
- Every retained negative fixture still fails for the same causal invariant; zero-fixture runners fail.
- No automatic workflow triggers, source generation commits or workflow writes.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
# Execute the task-specific registry/tooling fixtures; require a nonzero named test inventory.
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

Required engineering workflow is Rust/Cargo-only without weakening tests or reintroducing orchestration bloat.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
