# T19 — Expose the canonical provider protocol through daemon and CLI

**State:** NOT_STARTED; task packet only.  
**Stage:** 4. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T06, T17, T18.

## Observed gap

Primary commands use an ad-hoc tab-separated stream and local dispatch instead of the canonical negotiated provider surface and complete request/session binding.

## Exclusive write boundary

- `crates/search-provider-protocol/**`
- `bins/eliot-searchd/src/public_runtime_service.rs`
- `bins/eliot-searchd/src/authenticated_proxy.rs`
- `bins/eliot-searchd/src/service_output.rs`
- `bins/eliot-searchd/src/provider_composition.rs (new)`
- `bins/eliot-search/**`
- `bins/eliot-searchd/tests/provider_process.rs (new)`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- crates/search-provider-protocol/FUNCTIONS.md
- crates/search-ports/src/context.rs
- bins/eliot-searchd/src/public_runtime_service.rs
- bins/eliot-search/src/entry.rs
- T18 pairing API

## Required change

- Route versioned health/status/ingest/query/expand/cancel/shutdown through shared validated envelopes and one real daemon.
- Validate directional sequences, request IDs, bounded lengths, progress and terminal outcomes. Map all errors to closed typed provider reasons.
- CLI is protocol-only: it cannot open redb/Qdrant/CAS or acquire a competing owner. Preserve explicit local diagnostics with separate naming.
- Negotiate capabilities from T12 runtime evidence; unsupported recipes/modes return explicit unavailable, never empty successful output.

## Discriminating tests

- Real CLI/daemon process round trip including concurrent request identities, cancellation, reconnect and restart.
- Invalid version/sequence/length, duplicated terminal frame, out-of-order response, ordinary rejection and unknown mutation outcome.
- No client direct-storage access or hidden alternate-daemon fallback.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p eliot-searchd --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

One externally usable, versioned provider surface backs the supported CLI; product capabilities do not depend on obsolete shell protocols.

**Legacy cross-reference:** #61 #69 #74 #75 #76 #77 #78 #90. Historic issue instructions do not override the current contract.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
