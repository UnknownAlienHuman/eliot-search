# T01 — Reconcile execution authority, package inventory and stale assignments

**State:** IN_PROGRESS; bounded registry correction and initial inventory submitted, acceptance pending.  
**Stage:** 0. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** none; entry task.

## Current increment

See [reconciliation](../T01_RECONCILIATION.md), [legacy issue crosswalk](../LEGACY_ISSUES.md) and [machine inventory](../../../../swarm/reconciliation/2026-09-05.json).

- Corrected the missing normal `search-result-projector -> search-access` registry edge; kept a distinct dev-dependency out of the runtime graph.
- Enumerated 47 Cargo members against 45 registry entries; two unregistered adapters remain explicit blockers, not accepted additions.
- Mapped 38 open legacy issues to the existing task queue; no issue was closed as implemented.
- Recorded direct primary module declarations and eight isolation candidates. These are not a complete transitive reachability or reviewed move map.

Full Cargo metadata, per-package line counts, complete registry/module parity, contract/API evidence, exact T02 moves/contexts and independent review remain open. No handoff, gate, lease or downstream acceptance was issued. Rust execution is unavailable, not passing.

## Observed gap

Cargo contains later implementations and additional packages, while launch-state remains P00 with zero issued tickets/handoffs. Old issues duplicate work and contain obsolete package paths and conflicting design prescriptions.

## Exclusive write boundary

- `swarm/**`
- `docs/handoff/**`
- `AGENTS.md`
- `docs/execution/2026-09-05/**`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- Cargo.toml
- swarm/launch-state.toml
- swarm/crates.toml
- swarm/function-packets.toml
- docs/handoff/AUTHORITY_MAP.md

## Required change

- Inventory actual Cargo members, dependency edges, compiled targets, reachable modules and per-package handwritten lines. Classify implementation separately from executed/accepted evidence.
- Crosswalk all existing assignments and open issues to current functions and this program. Record superseded duplicates without marking unfinished implementations complete. Resolve #48 against the actual frozen contract pack.
- Reconcile package/feature/context registries through integration-owned edits. Issue bounded implementation contexts only after real prerequisites; preserve Part I and do not set fabricated PASS/G0/W0 receipts. Define one active writer per touched package.
- Record the exact file-move map needed to bring the daemon back under its line budget; T02 executes that map. New task packets are not immutable tickets or leases.

## Discriminating tests

- Registry parity against cargo metadata; every task owner and path exists or is explicitly approved as new.
- Every task has one causal exit, explicit dependency IDs and no dependency cycle; unknown evidence blocks advancement.
- Old #53/#59/#79 paths and #70/#85 duplicates cannot issue competing writer leases.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
# Execute the task-specific registry/tooling fixtures; require a nonzero named test inventory.
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

One reconciled execution index and accepted bounded contexts; no package is reimplemented solely because an obsolete issue says scaffold.

**Legacy cross-reference:** #23 #24 #48 #51 #53 #54 #55 #56 #59 #60 #61 #63 #64 #65 #68-#90 #93; historical PR #62 #66 #67 #94 #95 #96 are evidence leads, not current-head qualification. Historic issue instructions do not override the current contract.

## Execution and merge contract

This draft initially contained a task packet, not completed implementation. Keep it draft until all implementation and executed evidence requirements are met. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
