# T15 — Close deterministic representation profiles and coordinate maps

**State:** NOT_STARTED; task packet only.  
**Stage:** 3. **Role:** package:search-materializer.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T14.

## Observed gap

The shared UTF-8 transform exists; versioned representations, profile identities and coordinate/loss maps are not fully bound. Old issue #84 requests normalization that must not overwrite exact source coordinates.

## Exclusive write boundary

- `crates/search-prep/search-materializer/**`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- crates/search-prep/search-materializer/FUNCTIONS.md
- crates/search-prep/search-materializer/src/lib.rs
- accepted representation/coordinate contracts
- T14 revision readback API

## Required change

- Keep exact raw UTF-8 representation byte-preserving, including LF/CRLF/lone CR. Bind output to the verified revision and exact materialization profile.
- Implement only text encodings/transforms required by the accepted baseline contract; any BOM/transcoding/newline normalization creates a separate representation with exact maps and recorded loss.
- Define finite bytes/segments/lines/loss limits and cancellation; invalid or unsupported input returns typed gaps, not replacement-text exactness.
- Produce deterministic manifest data for storage by T16; this package performs no persistence or authorization and cannot invent a revision receipt.

## Discriminating tests

- Mixed endings, multibyte and non-BMP text, BOM/encoding boundaries for admitted profiles, malformed sequences and expansion overflow.
- Raw-to-representation and inverse range mapping golden fixtures; declared loss never disappears.
- Deterministic profile/version identity and cancellation without successful partial exact output.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p search-materializer --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

Every representation has explicit coordinate basis, verified source binding and bounded loss semantics; legacy raw offsets remain valid.

**Legacy cross-reference:** #84. Historic issue instructions do not override the current contract.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
