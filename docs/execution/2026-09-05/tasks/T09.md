# T09 — Make the persistent redb adapter bounded and port-complete

**State:** NOT_STARTED; task packet only.  
**Stage:** 2. **Role:** package:search-control-redb.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T04.

## Observed gap

PersistentControlJournal is real I/O, but each mutation copies and rereads the whole live record set. Arbitrary byte tags do not establish content-free schemas, and full operation-context integration is unfinished.

## Exclusive write boundary

- `crates/search-control-redb/**`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- crates/search-control-redb/src/persistent.rs
- crates/search-control-redb/src/persistent/codec.rs
- crates/search-control-redb/src/reference.rs
- crates/search-control-redb/FUNCTIONS.md
- crates/search-ports/src/control.rs

## Required change

- Implement the accepted control port/context wrapper with finite deadline/cancellation semantics and typed outcome-unknown recovery.
- Keep atomic record/generation/receipt commit. Replace unconditional whole-store mutation copies with bounded touched-record accounting and exact operation readback; retain an explicit bounded full consistency verifier.
- Enforce owned technical record codecs and declared limits before allocation; reject bodies, vectors and open schemas at capability-owned boundaries.
- Preserve exact-input replay after owner handoff, current snapshot publication fences and finite receipt retention. Do not introduce a second replay algorithm or silently prune protection.

## Discriminating tests

- Existing real-redb suite plus partial commit/readback faults, conflicting replay, stale generation, owner handoff and schema corruption.
- 10,000 reads produce no application writes; a small mutation does not scan/copy every value (instrumented adapter counters).
- Cancellation before dispatch is no-effect; after potential commit is unknown until exact recovery.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p search-control-redb --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

One usable bounded redb port implementation, tested against real files, ready for the primary runtime; not merely a faster reference model.

**Legacy cross-reference:** #70 #85. Historic issue instructions do not override the current contract.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
