# T13 — Connect canonical admission, identity and source registry to ingestion

**State:** NOT_STARTED; task packet only.  
**Stage:** 3. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T07, T11, T12.

## Observed gap

Primary ingestion hashes native identity and writes its own catalog rather than consuming canonical admission/registry receipts and residency policies.

## Exclusive write boundary

- `crates/search-source/search-source-admission/**`
- `crates/search-source/search-source-identity/**`
- `crates/search-source/search-source-registry/**`
- `bins/eliot-searchd/src/direct_store_ingest.rs`
- `bins/eliot-searchd/src/source_composition.rs (new)`
- `bins/eliot-searchd/tests/source_admission_process.rs (new)`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- crates/search-source/search-source-admission/FUNCTIONS.md
- crates/search-source/search-source-identity/FUNCTIONS.md
- crates/search-source/search-source-registry/FUNCTIONS.md
- bins/eliot-searchd/src/direct_store_ingest.rs

## Required change

- Compose existing kernels through accepted ports: current root/namespace policy, final-handle observation, stable identity, admitted membership and monotone source revision.
- Persist canonical registry changes in T11 transactions. Paths remain locators; replacement, hardlink aliases, rename and same-content distinct files remain distinguishable.
- Apply configured exclusions and explicit admission before retaining bytes; registration alone is not an access grant. Root unavailability is not an empty source set.
- Preserve legacy migration mappings; no fabricated source, revision, policy or admission receipt to make types compile.

## Discriminating tests

- Two equal-content files, rename, replacement at same path, hardlinks, cross-root/domain and stale-policy races.
- Denied source never reaches CAS; secret/generated/VCS policies follow explicit configuration.
- Restart preserves memberships, per-source revision occurrences and lineage bindings.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p eliot-searchd --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

Actual primary ingestion consumes canonical admitted source identities and registry state instead of an independent development catalog.

**Legacy cross-reference:** #53 #59 #60 #79 #80 #81. Historic issue instructions do not override the current contract.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
