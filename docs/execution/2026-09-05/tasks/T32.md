# T32 — Add exact no-execute Git source acquisition and lineage

**State:** NOT_STARTED; task packet only.  
**Stage:** 6. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T13, T14, T31.

## Observed gap

Primary filesystem ingestion and pure identity shapes do not implement admitted exact Git revisions, worktree lineage and object acquisition.

## Exclusive write boundary

- `crates/search-source/search-safe-reader/src/git.rs (new)`
- `crates/search-source/search-source-identity/**`
- `bins/eliot-searchd/src/source_composition.rs`
- `bins/eliot-searchd/tests/git_source_process.rs (new)`
- `Cargo.lock and package manifests for an explicitly qualified Rust Git dependency (integration only)`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- crates/search-source/{search-source-identity,search-safe-reader}/FUNCTIONS.md
- canonical repository/worktree lineage contracts
- T13 admission API
- T14 revision CAS

## Required change

- Read exact admitted local Git objects with bounded decompression and hash/type/length verification, including required packed-object support through qualified Rust libraries.
- Never invoke hooks, checkout filters, shell, credentials or network. Missing unsupported object forms return explicit gaps, never live-path substitution.
- Bind repository/worktree/submodule/fork/mirror relationships to evidence; same path/remote/name/HEAD is not identity.
- Export immutable admitted revisions into canonical CAS with original owner/lineage, not a second mutable catalog.

## Discriminating tests

- Loose and packed blob/tree/commit/tag, corrupt pack/delta/decompression overflow, detached worktree and submodule.
- Malicious hooks/filters demonstrably not invoked; network disabled.
- Same remote URL with different repository lineage, missing object and namespace transfer refusal.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p eliot-searchd --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

Supported Git inputs use exact immutable objects and explicit lineage through the same source pipeline, without executing repository content.

**Legacy cross-reference:** #82. Historic issue instructions do not override the current contract.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
