# T42 — Make optional workers and leaf adapters honestly unavailable by default

**State:** NOT_STARTED; task packet only.  
**Stage:** 7. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T19, T36.

## Observed gap

Both worker entrypoints are empty main functions. Optional model/document/leaf packages must not be mistaken for executed baseline features.

## Exclusive write boundary

- `bins/eliot-search-model-worker/**`
- `bins/eliot-search-doc-worker/**`
- `crates/search-model-provider/**`
- `crates/search-eliot-adapter/**`
- `crates/search-research-export-adapter/**`
- `swarm/w10-optional-depth.toml`
- `qualification/optional-depth/W10_QUALIFICATION.md`
- `primary capability/packaging registry from T12`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- both worker src/main.rs files
- swarm/w10-optional-depth.toml
- qualification/optional-depth/W10_QUALIFICATION.md
- leaf-adapter FUNCTIONS.md
- T12 capability policy

## Required change

- Replace silent successful no-op worker invocation with bounded explicit unsupported/disabled behavior and nonzero exit; exclude unqualified workers from default product launch.
- Keep model/document/advanced-depth activation behind existing exact ADR/artifact/benefit gates; this task does not implement optional inference/OCR or call paid services.
- Verify leaf adapter boundaries: no reverse authority, canonical client writes, second source catalog, raw Qdrant access or implicit online-research fallback.
- Wire supported leaf translation to the canonical provider or advertise it unavailable. Configuration/compilation alone cannot enable optional capabilities.

## Discriminating tests

- Default install invokes no worker/model network call; worker direct launch cannot report success while doing nothing.
- Forced optional flag without accepted evidence fails closed; profile removal returns to baseline cleanly.
- Boundary fixtures for export provenance, prohibited authority escalation and no client-store credentials.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p eliot-searchd --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

Optional placeholders are not shipped or advertised as working; baseline completion is explicitly separated from future optional implementation.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
