# T14 — Bind immutable revision CAS to full residency domains

**State:** NOT_STARTED; task packet only.  
**Stage:** 3. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T13.

## Observed gap

Existing immutable objects are keyed by legacy source/content identity, not the complete canonical scope/access/confidentiality/key/retention/erasure residency key.

## Exclusive write boundary

- `crates/search-source/search-revision-store/**`
- `crates/search-runtime/search-revision-crypto/**`
- `crates/search-runtime/search-os-secrets-windows/**`
- `bins/eliot-searchd/src/revision_protection*.rs`
- `bins/eliot-searchd/src/secure_revision_writer.rs`
- `bins/eliot-searchd/src/secure_direct_store*.rs`
- `bins/eliot-searchd/tests/canonical_revision_process.rs (new)`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- crates/search-source/search-revision-store/FUNCTIONS.md
- bins/eliot-searchd/src/revision_protection.rs
- bins/eliot-searchd/src/secure_revision_writer.rs
- Architecture S6.3 and revision/residency contracts

## Required change

- Construct real scoped CAS adapters behind the revision-store kernel and accepted residency port. Include every domain and versioned digest in physical reuse identity.
- Preserve existing DPAPI envelope compatibility during explicit migration; choose no new cipher by copying old #83. Any crypto/profile change needs approved ADR and executed qualification.
- Retain exact immutable bytes and monotone revision occurrences, encrypt before publication, no-clobber publish and authenticate readback before issuing real receipts.
- Handle orphans and unknown writes with bounded recovery. Shared bytes across inequivalent security/lifecycle domains must never be physically reused.

## Discriminating tests

- Same content across each differing residency domain produces separate storage identity; equivalent allowed reuse verifies exact bytes.
- Wrong key/domain/digest/length, truncated envelope, interrupted publication, lost credential and duplicate operation.
- Historical range read after source deletion/restart plus sentinel check for new plaintext residue.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p eliot-searchd --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

Primary retained revisions have real canonical residency/readback receipts; no global content-only CAS or digest-type substitution.

**Legacy cross-reference:** #83. Historic issue instructions do not override the current contract.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
