# T36 — Execute baseline recipes, subject resolution and comparisons

**State:** NOT_STARTED; task packet only.  
**Stage:** 6. **Role:** integration.  
**Audited base:** `a5abdf7ef0cb9d691000759494fd8829b2ba0b60`. **Depends on:** T28, T34, T35.

## Observed gap

Foundational types and pure subject/comparison functions are not user-executable baseline recipes through the primary provider.

## Exclusive write boundary

- `crates/search-query/search-subject-resolver/**`
- `crates/search-query/search-comparator/**`
- `crates/search-query/search-query-planner/**`
- `bins/eliot-searchd/src/query_composition.rs`
- `bins/eliot-searchd/src/provider_composition.rs`
- `bins/eliot-searchd/tests/recipes_process.rs (new)`

Paths labelled new are proposed files, not claims that they exist. Do not change unrelated files under a wildcard.

## Bounded additional read set

- crates/search-query/{search-subject-resolver,search-comparator,search-query-planner}/FUNCTIONS.md
- canonical recipe registry
- T28 validated candidate API
- T34 exact proofs
- T35 structural facts

## Required change

- Map every required baseline recipe to actual prepared sources, bounded query legs, structural/exact evidence and result projection.
- Preserve ambiguity sets, analogue ladders, lineage collapse and descriptive evidence-role comparisons; never select a normative correct implementation without authority.
- Use the one existing source/index owner and shared provider surface. Leaf clients do not introduce reverse authority, online research or canonical memory writes.
- Advertise only recipes whose complete dependency chain executes; derive a coverage table directly from the accepted recipe registry.

## Discriminating tests

- Ambiguous name, same path in different repos, conflicting lineage and incomplete reference portfolio.
- Each baseline recipe has at least one positive, one denied/partial and one unsupported-input end-to-end fixture.
- Comparison statements reference exact source evidence and retain theory/uncertainty distinctions required by contract.

```sh
cargo +1.98.0 check --workspace --all-targets --all-features --locked
cargo +1.98.0 test --locked -p eliot-searchd --all-targets
```

Run the named new scenarios as well as existing tests; these commands alone do not prove live/native coverage. Full fmt/Clippy/rustdoc and exact-head verification follow the program gate.

## Exit condition

All required baseline recipes execute through the real primary provider with bounded, provenance-backed outputs, not dispatch stubs.

## Execution and merge contract

This draft initially contains a task packet, **not the implementation**. Keep it draft until implementation and executed evidence are appended. Do not merge a packet-only PR as completed work.

One writer/worktree per task; lock every touched package, including shared composition files. Before assignment, the integration owner resolves proposed/moved paths to an exact bounded context and write scope. Refresh from current `main` after predecessor acceptance. Read root/nearest `AGENTS.md`, `docs/handoff/AUTHORITY_MAP.md`, the relevant registry fragments and accepted public dependency handoffs. Part I remains normative; this packet is not a ticket or lease. Cross-package/root edits require the integration owner.

Start with failing discriminating tests and preserve regressions. No fabricated IDs/digests/receipts, mock-as-live proof, silent fallback, second search database, Python product runtime or automatic CI. Record exact base/head, changed paths, commands/exit codes, platform/toolchain and named test counts. Zero tests or skipped required native/live fixtures are **not PASS**. Independent reviewer verifies the proposed head. Post-merge readback must precede downstream release. A missing dependency/path is resolved by T01, not guessed.
