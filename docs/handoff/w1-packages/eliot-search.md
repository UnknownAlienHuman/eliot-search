# W1 package checkpoints — `eliot-search`

**Write scope:** `bins/eliot-search/**`  
**Authority:** none; an issued ticket, active lease and acknowledged context are still required.

Read only the materialized package context, exact accepted dependency handoffs, package assignment,
`FUNCTIONS.md`, this packet and the common rules in `../W1_MILESTONE_PACKETS.md`.

## L0 — CLI boundary

Implement bounded argument/input validation, config/profile selection, no direct store/daemon internals, and deterministic exit/error schema.

## L1 — Connection and pairing

Implement endpoint discovery only through accepted protocol configuration, pairing/session establishment, capabilities, and authentication failures.

## L2 — Requests and rendering

Implement exact recipe request construction, progress and typed terminal rendering, cancellation, deadlines, and content/disclosure bounds.

## L3 — Failure matrix and handoff

Close disconnect/replay/backpressure/exit-code/compatibility tests, no-direct-store guard, line budget, and package submission evidence.

## Exit rule

At every checkpoint record failing-first tests, exact commands/raw outcomes, typed gaps, package-only
diff, dependency/API digests and line count. The fourth checkpoint creates only a submission candidate;
independent review and integration-owned handoff remain separate.
