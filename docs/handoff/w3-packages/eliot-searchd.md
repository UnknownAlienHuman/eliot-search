# W3 package checkpoints — `eliot-searchd`

**Write scope:** `bins/eliot-searchd/**`  
**Authority:** none; an issued ticket, active lease, acknowledged replacement context and accepted W2 daemon handoff/receipt are still required.

Read only the materialized W3 daemon context, accepted prior daemon/W2_G1 receipts, exact W3 library handoffs, exact Qdrant qualification receipts, assignment, `FUNCTIONS.md`, this packet and common rules in `../W3_MILESTONE_PACKETS.md`.

## IDX0 — Re-entry and profile closure

Verify replacement-context semantics, accepted W2 daemon API, W2_G1 receipt, exact W3 library/API digests, qualified artifact/client/schema set and compiled feature graph. Prior W1/W2 packets are not replayed.

## IDX1 — Process/data composition

Compose private supervisor/bridge adapters, lexical/projection/pin/publication/reclaimer ports and exact data-root/config identities. No raw Qdrant client reaches capability packages.

## IDX2 — Readiness and DIRECT fallback

Publish indexed capability only when process, schema, handlers, route and accepted receipts are coherent. Qdrant failure truthfully retains DIRECT mode and explicit degradation.

## IDX3 — Recovery, drain and handoff candidate

Close startup/publication/process-loss recovery, route/pin cleanup, reverse shutdown, content/secret audit, line budget and daemon package submission evidence.

## Exit rule

Each checkpoint records failing-first tests, exact commands/raw outcomes, unavailable checks, package-only diff, prior-stage/dependency/qualification digests and line count. IDX3 creates only a submission candidate; independent review, package handoff and W3 integration receipt remain separate.
