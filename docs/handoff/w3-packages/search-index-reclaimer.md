# W3 package checkpoints — `search-index-reclaimer`

**Write scope:** `crates/search-index-qdrant/search-index-reclaimer/**`  
**Authority:** none; an issued ticket, active lease and acknowledged context are still required.

Read only the materialized package context, exact accepted dependency handoffs, applicable pin/reclaim probes, package assignment, `FUNCTIONS.md`, this packet and common rules in `../W3_MILESTONE_PACKETS.md`.

## IR0 — Reclaim candidates and policy

Implement exact retired-point manifest validation, reclaim policy/configuration and separation from retention/security purge. Broad filter deletion is forbidden.

## IR1 — Pin-safe planning

Implement active route/epoch pin checks, exact protected sets, finite batches and deterministic reclaim plans with no mutation.

## IR2 — Delete, acknowledge and recover

Implement exact point deletion through ports, acknowledgement/readback, cancellation, unknown outcomes, retry identity and residual manifest handling.

## IR3 — Conformance and handoff candidate

Close active-pin refusal, exact-delete, broad-filter prohibition, crash/readback, purge-separation and resource fixtures, line budget and package submission evidence.

## Exit rule

Each checkpoint records failing-first tests, exact commands/raw outcomes, unavailable checks, package-only diff, dependency/pin/profile digests and line count. IR3 creates only a submission candidate; independent review and integration-owned handoff remain separate.
