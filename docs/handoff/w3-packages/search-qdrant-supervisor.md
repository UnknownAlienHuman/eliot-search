# W3 package checkpoints — `search-qdrant-supervisor`

**Write scope:** `crates/search-index-qdrant/search-qdrant-supervisor/**`  
**Authority:** none; an issued ticket, active lease, acknowledged context and exact qualified artifact are still required.

Read only the materialized package context, exact accepted dependency handoffs, qualified artifact/process inputs, package assignment, `FUNCTIONS.md`, this packet and common rules in `../W3_MILESTONE_PACKETS.md`.

## QS0 — Artifact and process identity

Implement exact server artifact/build/executable/data-root/process-incarnation validation and configuration floors. PID, path or health alone never proves ownership.

## QS1 — Start and authenticated health

Implement bounded process launch through private adapters, loopback binding, purpose-bound secret use, job/ACL containment and exact authenticated readiness observations.

## QS2 — Recovery and quarantine

Implement process-loss, PID-reuse, artifact-replacement, bounded restart, cancellation/deadline and unknown-outcome recovery. Ambiguous identity quarantines rather than attaches.

## QS3 — Qualification and handoff candidate

Close Windows process/fault fixtures, secret side-channel audit, shutdown cleanup, no-auto-download/upgrade guard, line budget and package submission evidence.

## Exit rule

Each checkpoint records failing-first tests, exact commands/raw outcomes, unavailable checks, package-only diff, dependency/artifact/profile digests and line count. QS3 creates only a submission candidate; independent review and integration-owned handoff remain separate.
