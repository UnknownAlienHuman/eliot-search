# W2 package checkpoints — `search-materializer`

**Write scope:** `crates/search-prep/search-materializer/**`  
**Authority:** none; accepted G0/W1, issued ticket, active lease and acknowledged context remain required.

Read only the materialized package context, exact accepted dependency handoffs, assignment,
`FUNCTIONS.md`, this packet and the common rules in `../W2_MILESTONE_PACKETS.md`.

## M0 — Profile, request and revision input

Implement exact materializer profile identity, bounded request validation and exact retained-revision acquisition with no current-path substitution.

## M1 — Decode and normalization

Implement explicit encoding/BOM/invalid-sequence, newline and Unicode rules with finite expansion and deterministic output.

## M2 — Maps, loss and assurance

Implement coordinate/loss maps, assurance ceilings, complete transformation accounting, canonical representation and verification.

## M3 — Determinism and optional-boundary closure

Close encoding/map goldens, cancellation/oversize behavior, no-execute/no-network guards, optional-provider nonselection, line budget and submission evidence.

## Exit rule

Each checkpoint records failing-first tests, exact commands/raw outcomes, typed gaps, package-only diff,
dependency/API digests and line count. M3 creates only a package submission candidate.
