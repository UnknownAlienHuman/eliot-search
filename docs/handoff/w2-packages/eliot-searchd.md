# W2 package checkpoints — `eliot-searchd`

**Write scope:** `bins/eliot-searchd/**`  
**Authority:** none; accepted G0/W1, issued ticket, active lease and acknowledged W2 context remain required.

This is a re-entry assignment. Read only the new W2 materialized context, the accepted prior W1 daemon
API/handoff and W1 receipt, exact accepted W2 library handoffs, assignment, `FUNCTIONS.md`, this packet
and the common rules in `../W2_MILESTONE_PACKETS.md`. Do not replay the W1 implementation packet.

## D20 — Re-entry profile and dependency fence

Verify prior W1 daemon identity, accepted W1 receipt, exact W2 library API digests, DIRECT-only feature graph and absence of indexed/query profiles.

## D21 — Source-spine composition

Bind admission, identity, registry, safe-reader, revision, materializer and unitizer ports through private adapters without duplicating package logic.

## D22 — DIRECT readiness and request path

Publish coherent DIRECT readiness/capabilities and source/preparation handlers only when all exact dependencies are live; preserve typed gaps and bounded requests.

## D23 — Recovery, drain and handoff closure

Close startup/reconfiguration/crash/recovery/drain/shutdown fixtures, no Qdrant construction, package-only diff, line budget and submission evidence.

## Exit rule

Each checkpoint records failing-first tests, exact commands/raw outcomes, typed gaps, package-only diff,
dependency/API digests and line count. D23 creates only a package submission candidate.
