# W2 package checkpoints — `search-source-identity`

**Write scope:** `crates/search-source/search-source-identity/**`  
**Authority:** none; accepted G0/W1, issued ticket, active lease and acknowledged context remain required.

Read only the materialized package context, exact accepted dependency handoffs, assignment,
`FUNCTIONS.md`, this packet and the common rules in `../W2_MILESTONE_PACKETS.md`.

## I0 — Filesystem profiles and path keys

Implement exact filesystem identity profiles, bounded observations, canonical lookup keys and rejection of path-only identity.

## I1 — Source and binding transitions

Implement source identity resolution, rename/move/path-reuse transitions, hard-link grouping, binding history and ambiguity.

## I2 — Repository and workspace lineage

Implement repository/worktree/submodule boundaries, fork/mirror/proven-copy relations, workspace identities, batch resolution and cancellation.

## I3 — Property and disclosure closure

Close case/Unicode/physical-ID matrices, history properties, nested-boundary fixtures, redacted views, line budget and submission evidence.

## Exit rule

Each checkpoint records failing-first tests, exact commands/raw outcomes, typed gaps, package-only diff,
dependency/API digests and line count. I3 creates only a package submission candidate.
