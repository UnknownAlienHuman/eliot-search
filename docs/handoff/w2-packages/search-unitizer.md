# W2 package checkpoints — `search-unitizer`

**Write scope:** `crates/search-prep/search-unitizer/**`  
**Authority:** none; accepted G0/W1, issued ticket, active lease and acknowledged context remain required.

Read only the materialized package context, exact accepted dependency handoffs, assignment,
`FUNCTIONS.md`, this packet and the common rules in `../W2_MILESTONE_PACKETS.md`.

## U0 — Profile, request and boundaries

Implement exact unitizer profile identity, bounded request validation, deterministic boundary scanning and no fake complete output.

## U1 — Occurrences, IDs and anchors

Implement occurrence descriptors, domain-separated unit IDs, native/canonical anchor validation and conditional predicate assurance.

## U2 — Manifests, diffs and batches

Implement canonical ordered manifests, complete represented/omitted accounting, exact retained/created/retired diffs and batch cancellation.

## U3 — Property and non-overclaim closure

Close deterministic/golden properties, duplicate/conflict negatives, cfg unknown preservation, no semantic/ranking/index ownership, line budget and submission evidence.

## Exit rule

Each checkpoint records failing-first tests, exact commands/raw outcomes, typed gaps, package-only diff,
dependency/API digests and line count. U3 creates only a package submission candidate.
