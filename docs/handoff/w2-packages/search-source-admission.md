# W2 package checkpoints — `search-source-admission`

**Write scope:** `crates/search-source/search-source-admission/**`  
**Authority:** none; accepted G0/W1, issued ticket, active lease and acknowledged context remain required.

Read only the materialized package context, exact accepted dependency handoffs, assignment,
`FUNCTIONS.md`, this packet and the common rules in `../W2_MILESTONE_PACKETS.md`.

## A0 — Policy schema and purity

Implement bounded schema validation, canonical policy normalization/fingerprint, closed rules/reasons, configuration validation and no-I/O guards.

## A1 — Observation and classification

Implement bounded source observations, explicit unavailable signals, generated/vendor/binary/system/sensitivity classification and deterministic observation identity.

## A2 — Decision and receipts

Implement deny-by-default evaluation, stable ordered reasons, immutable receipt construction/verification, batch accounting, cancellation and budget semantics.

## A3 — Change and conformance closure

Close restrictive/permissive change classification, sensitive-source fixtures, unknown-field/override negatives, disclosure audit, line budget and submission evidence.

## Exit rule

Each checkpoint records failing-first tests, exact commands/raw outcomes, typed gaps, package-only diff,
dependency/API digests and line count. A3 creates only a package submission candidate.
